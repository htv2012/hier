# hier

*hier* is a python library for printing hierarchical data such as file system, JSON, YAML, XML, and TOML.

## Installation

    pip install hier

## Command-Line Usage

```
$ hier [file or dir]
```

As of version 1.1, *hier* supports files of types JSON, TOML, XML, and YAML 

